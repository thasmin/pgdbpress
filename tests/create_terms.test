CREATE TABLE wp_terms ( term_id bigint(20) unsigned NOT NULL auto_increment, name varchar(200) NOT NULL default '', slug varchar(200) NOT NULL default '', term_group bigint(10) NOT NULL default 0, PRIMARY KEY  (term_id), KEY slug (slug(255)), KEY name (name(255))) DEFAULT CHARACTER SET latin1 COLLATE latin1_swedish_ci;
CREATE TABLE wp_terms ( "term_id" SERIAL NOT NULL, "name" VARCHAR(200) NOT NULL DEFAULT '', "slug" VARCHAR(200) NOT NULL DEFAULT '', "term_group" BIGINT NOT NULL DEFAULT 0 , PRIMARY KEY ("term_id") ); CREATE INDEX wp_terms_slug ON wp_terms ( "slug" ); CREATE INDEX wp_terms_name ON wp_terms ( "name" );
